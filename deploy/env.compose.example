# deploy/.env — read by `docker compose --env-file deploy/.env`.
# setup-vps.sh writes this file.

ROOT_DOMAIN=bzaro.in
ACME_EMAIL=you@bzaro.in

# Must match the values inside /srv/bzaro/app/.env
POSTGRES_PASSWORD=
SRH_TOKEN=
